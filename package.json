{
  "name": "investing-stats-cron",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "underscore": "^1.13.2"
  },
  "devDependencies": {
    "jest": "^27.5.1"
  }
}